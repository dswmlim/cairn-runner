# docs

`gameplay.gif` lives here.

The committed copy is a **synthetic preview** generated headlessly by
`tools/render_gif.py` (Python + Pillow) — a faithful re-render of the engine,
not a browser screen recording. To replace it with real footage, follow
"Generate a real gameplay GIF" in the root README.
